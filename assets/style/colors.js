import { StyleSheet } from "react-native";

const colors = StyleSheet.create({
  primary: "#384766",
  secondary: "#345499",
  success: "#2DF141",
  danger: "#FF3838",
  white: "#fff",
});
export default colors;
